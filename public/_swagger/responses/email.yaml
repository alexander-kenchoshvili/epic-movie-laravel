components:
  responses:
    Email:
      "200":
        description: OK
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - $ref: "../schemas/email.yaml#/components/schemas/Email"
      "404":
        allOf:
          - $ref: "../common.yaml#/components/responses/404"
